package agents;

import jade.core.Agent;

public class GodlikeAgent extends Agent {
    public GodlikeAgent() {
    }

    protected void setup() {
        //create map with messages to handle with, for regions
        //we have to load files from some directory, and for each file
        //1) create params map
        //2) create "peoples" map
        //3) create region, pass all arguments to it
        //invoke migration for all "peoples"
    }
}
